# NOTE: You're on Python 3.14, which Django 5.0/5.1 don't support (breaks the
# admin — ticket #35844). apps/common/compat.py backports the fix so this pin
# keeps working. Permanent fix: move to Django>=5.2 (first version with 3.14
# support) and delete apps/common/compat.py, or run the backend on Python 3.12.
Django>=5.0,<5.2
djangorestframework>=3.15,<3.16
djangorestframework-simplejwt>=5.3,<5.4
psycopg[binary]>=3.1,<4.0
django-environ>=0.11,<0.12
django-cors-headers>=4.3,<5.0
django-filter>=24.1
drf-spectacular>=0.27,<0.28
celery>=5.3,<6.0
redis>=5.0,<6.0
django-celery-beat>=2.6,<2.7
sentry-sdk>=2.0,<3.0
django-csp>=3.8,<4.0
Pillow>=10.0
# Rich-text ticket replies/messages + HTML sanitization.
django-ckeditor-5>=0.2.13
nh3>=0.2.17
